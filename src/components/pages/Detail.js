import React from "react";
import { Typography } from "@mui/material";

const Detail = ({ restId }) => {
  return (
    <>
      <Typography>I'm the Detail page {restId} </Typography>
    </>
  );
};
export default Detail;
